*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/

# Used by dotenv library to load environment variables.
# .env

# Ignore Byebug command history file.
.byebug_history

## Specific to RubyMotion:
.dat*
.repl_history
build/
*.bridgesupport
build-iPhoneOS/
build-iPhoneSimulator/

## Specific to RubyMotion (use of CocoaPods):
#
# We recommend against adding the Pods directory to your .gitignore. However
# you should judge for yourself, the pros and cons are mentioned at:
# https://guides.cocoapods.org/using/using-cocoapods.html#should-i-check-the-pods-directory-into-source-control
#
# vendor/Pods/

## Documentation cache and generated files:
/.yardoc/
/_yardoc/
/doc/
/rdoc/

## Environment normalization:
/.bundle/
/vendor/bundle
/lib/bundler/man/

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
# Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Used by RuboCop. Remote config files pulled in from inherit_from directive.
# .rubocop-https?--*

# iOS Certificates and Provisioning Profiles (sensitive data)
certificates/*
profiles/*
!certificates/.gitkeep
!profiles/.gitkeep

# Configuration files with sensitive credentials
certificates/config.env

# Local development artifacts and auto-generated files
.claude/settings.local.json
app/fastlane/Fastfile
app/fastlane/report.xml

# Backup files and temporary files
*.backup
*_backup
*.bak
config.env.backup*
certificates/config.env.backup*
*.tmp
*.temp
.DS_Store

# Deployment logs (can be large and sensitive)
deployment_*.log
fastlane_*.log

# Test iOS projects and template apps (sensitive/personal data)
template_swiftui/
*_swiftui/
test_app/
example_app/

